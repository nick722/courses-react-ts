import React from 'react';
import Button from '../../../../common/Button/Button';

const SEARCH_BAR_BUTTON_TEXT = 'Search';
const SearchCoursesButton = () => {
	return <Button onClick={() => void 0}>{SEARCH_BAR_BUTTON_TEXT}</Button>;
};

export default SearchCoursesButton;
